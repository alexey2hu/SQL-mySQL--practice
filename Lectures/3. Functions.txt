
# Функции

/*
** Язык SQL не чувствителен к регистру,
** но хорошим тоном считается писать ключевые слова SQL большими буквами
*/

-- Выбор базы данных ( ';' обязательный разделитель при нескольких запросах)
USE sql_store;

SELECT *										(Выбрать)
FROM customers							(Из (таблицы))
WHERE customer_id > 1				(Где (условие))
ORDER BY first_name;				(Отсортировать по)
ы
-- SELECT 1,2