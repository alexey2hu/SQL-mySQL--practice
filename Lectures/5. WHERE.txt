# WHERE customer_id > 1 (Где (условие))

/*
** Операторы сравнения ('=', '>', '<', '>=', '<=', '<>' или '!=' )
** 
** '<>' = '!='
**
** Имеют не строгое равенство
*/

# По соглашению используются одинарные кавычки в строках и датах ''
# Пример со стандартом даты:
	SELECT *
	FROM customers
	WHERE birth_date > '1990-01-01';

# EXERCISE:
	-- Вернуть заказы сделанные в 2019 году
	
	# Выполнение:
		SELECT *
		FROM orders
		WHERE order_date >= '2019-01-01' && order_date < '2020-01-01';